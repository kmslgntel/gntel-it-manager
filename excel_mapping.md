# Excel Mapping — 월간 서버 점검리스트 양식 셀 매핑

이 문서는 정기점검 Excel Export 구현을 위한 셀 매핑 명세입니다.
기준 양식 파일: `●월간 서버 점검리스트_2026년_4월.xlsx` (프로젝트 루트)

Claude Code는 이 매핑을 그대로 사용한다. 양식을 다시 분석할 필요 없다.

---

## 1. 시트 구조

- 시트명: 월 이름 (예: `4월`). Export 시 해당 월 이름으로 시트를 생성하거나 원본 시트명을 교체한다.
- 크기: 약 23행 x 11열 (A~K)
- 원본 파일은 수정하지 않는다. 복사본을 만들어 데이터를 채운다.
- 결과 파일명 예: `월간_서버_점검리스트_2026년_05월.xlsx`

---

## 2. 병합 셀 목록 (그대로 유지)

```
A1:K1     제목
A2:B2     년월
D2:K2     (점검일자 우측 영역)
A6:A9     H/W (점검사항 세로 병합)
A11:A12   보안 업데이트
A13:A15   계정
A17:A19   수신메일 현황
A20:B23   점검결과 (라벨)
C20:K23   점검결과 (내용 입력 영역)
```

병합 셀에 값을 쓸 때는 좌상단 셀(예: C20)에만 기록한다.

---

## 3. 헤더 영역

| 셀 | 내용 | Export 시 |
|----|------|-----------|
| A1 | `월간 서버 점검리스트` | 고정 |
| A2 | `2026년 4월` | 해당 년월로 치환 (예: `2026년 5월`) |
| C2 | `점검일자 : 5/11` | 해당 점검일자로 치환 |

---

## 4. 서버 열 매핑 (C3~K3)

점검 대상 서버는 C열부터 K열까지 9개. **이 순서와 명칭을 고정한다.**

| 열 | 서버명 (Excel 고정값) |
|----|----------------------|
| C | 그룹웨어 |
| D | ERP |
| E | 파일서버 |
| F | 방화벽(VPN) |
| G | SPAMOUT |
| H | DNS, Homepage |
| I | 백업서버 |
| J | ICARUS |
| K | HCI |

> 서버 마스터(Server 모델)의 name 또는 export용 라벨은 위 명칭과 1:1로 매칭되어야 한다.
> 내부 식별자와 Excel 표시명이 다르면, Server 모델에 `excel_label` 같은 필드로 매핑하거나
> 코드 내 매핑 딕셔너리(server_name → 열 index)를 둔다.

---

## 5. 점검 항목 행 매핑

A열=점검사항(대분류), B열=세부 점검사항. 데이터는 각 서버 열(C~K)에 들어간다.

| 행 | A열(대분류) | B열(세부) | 모델 필드 (InspectionDetail) | 값 형식 |
|----|------------|----------|------------------------------|---------|
| 4  | IP | IP 상태 확인 | ip_status | `정상` / `불일치` 등 텍스트 |
| 5  | 시스템 | Windows Event Log | event_log_summary | `특이사항 없음` 등 텍스트 |
| 6  | H/W | CPU | cpu_total_ghz + cpu_usage_pct | `2.1Gh /  9% 사용` 형식 문자열 |
| 7  | (H/W) | RAM | ram_total_gb + ram_used_gb | `64GB / 16GB 사용` 형식 |
| 8  | (H/W) | HDD | disk_info_json | 디스크별 줄바꿈(`\n`) 텍스트 (아래 6.1 참고) |
| 9  | (H/W) | Disk 상태 | disk_health | `특이사항 없음` 등 텍스트 |
| 10 | Backup | 최종 백업일 | last_backup_date | 날짜 (datetime, 예 2026-05-10) |
| 11 | 보안 업데이트 | Windows 업데이트 | win_update_status | `최신버전 적용` 등 텍스트 |
| 12 | (보안) | V3 | v3_version | `최신버전 적용` 등 텍스트 |
| 13 | 계정 | 생성 | account_create_cnt | 숫자 또는 `-` |
| 14 | (계정) | 권한변경 | account_change_cnt | 숫자 또는 `-` |
| 15 | (계정) | 삭제 | account_delete_cnt | 숫자 또는 `-` |
| 16 | IPS | 차단/시도 | ips_block_cnt (+ 시도) | `38,166/62,496` 형식 (차단/시도) |
| 17 | 수신메일 현황 | 정상 | mail_normal_cnt | 숫자 |
| 18 | (수신메일) | 차단(스팸/바이러스/랜섬웨어) | mail_spam_cnt, mail_virus_cnt, mail_ransom_cnt | `30,645\n(30,192/352/101)` 형식 |
| 19 | (수신메일) | 합계 | (자동 계산) | 숫자 (정상+차단 합계) |
| 20 | 점검결과 | (병합 A20:B23) | — | 라벨 |

### 점검결과 내용
- 입력 위치: **C20** (병합 C20:K23)
- 모델 필드: `Inspection.remarks`
- 형식: 여러 줄 텍스트. 예:
  ```
  - 그룹웨어 등록 1(플랫폼사업팀)
  - 스팸 차단 시스템 차단 필터 4건 업데이트 (견적 사칭 메일 스팸, 사내 재무팀 사칭 메일 차단 외 2건)
  - Windows 보안 업데이트 완료 (5/10)
  ```

---

## 6. 값 형식 주의사항 (중요)

Excel 셀 값은 단순 숫자가 아니라 **포맷된 문자열**인 경우가 많다.
모델에는 정규화된 값으로 저장하되, Export 시 아래 형식 문자열로 조합한다.

### 6.1 HDD (8행) — 디스크별 줄바꿈 텍스트
- 한 서버에 여러 디스크. 줄바꿈(`\n`)으로 구분.
- 예(그룹웨어 C8):
  ```
   C: 249GB / 111GB 사용
   D: 249GB / 99GB 사용
   E: 1.99TB / 860GB 사용
   G: 0.99TB / 255GB 사용
  ```
- 모델 `disk_info_json`에는 JSON 배열로 저장 권장:
  ```json
  [{"drive":"C","total":"249GB","used":"111GB"}, ...]
  ```
- Export 시 ` {drive}: {total} / {used} 사용` 형태로 줄바꿈 조합.
- 셀에 줄바꿈 적용하려면 `cell.alignment = Alignment(wrap_text=True)` 필요.

### 6.2 CPU (6행)
- 형식: `{전체}Gh /  {사용}% 사용` (예: `2.1Gh /  9% 사용`)
- 일부 서버는 GHz 없이 `2% 사용`만 있기도 함 (방화벽 F6, SPAMOUT G6). 빈 값 허용.
- HCI(K6)는 `363.Gh /  67% 사용`처럼 큰 값.

### 6.3 RAM (7행)
- 형식: `{전체}GB / {사용}GB 사용` (예: `64GB / 16GB 사용`)
- 단위가 TB인 경우도 있음 (HCI K7: `1.47TB / 490GB 사용`). 단위 포함 문자열로 처리.

### 6.4 IPS (16행)
- 형식: `{차단}/{시도}` (예: `38,166/62,496`)
- 천 단위 콤마 포함. 보통 방화벽(F열)에만 값이 있음.

### 6.5 수신메일 차단 (18행)
- 형식: `{차단합계}\n({스팸}/{바이러스}/{랜섬웨어})` (예: `30,645\n(30,192/352/101)`)
- 보통 SPAMOUT(G열)에만 값.

### 6.6 빈 값 처리
- 해당 서버에 해당 항목이 없으면 `-` 또는 빈칸.
- 계정(13~15행)은 값 없으면 `-`로 표시하는 것이 원본 관례.

### 6.7 최종 백업일 (10행)
- datetime 값. Export 시 날짜 형식(yyyy-mm-dd 또는 원본 셀 서식)으로.

---

## 7. 구현 권장 방식

1. 원본 양식을 `openpyxl.load_workbook`으로 로드 (복사본).
2. 헤더(A2 년월, C2 점검일자) 치환.
3. 서버명 → 열 매핑 딕셔너리:
   ```python
   SERVER_COL = {
       '그룹웨어': 'C', 'ERP': 'D', '파일서버': 'E', '방화벽(VPN)': 'F',
       'SPAMOUT': 'G', 'DNS, Homepage': 'H', '백업서버': 'I',
       'ICARUS': 'J', 'HCI': 'K',
   }
   ```
4. 항목 → 행 매핑 딕셔너리:
   ```python
   ITEM_ROW = {
       'ip_status': 4, 'event_log': 5, 'cpu': 6, 'ram': 7, 'hdd': 8,
       'disk_health': 9, 'backup': 10, 'win_update': 11, 'v3': 12,
       'acct_create': 13, 'acct_change': 14, 'acct_delete': 15,
       'ips': 16, 'mail_normal': 17, 'mail_block': 18, 'mail_total': 19,
   }
   ```
5. InspectionDetail을 순회하며 `ws[f'{col}{row}'] = value` 로 채움.
6. 점검결과는 `ws['C20'] = inspection.remarks`.
7. 줄바꿈 들어가는 셀(8, 18행)은 `wrap_text=True`.
8. 합계(19행)는 정상+차단 자동 계산.
9. 양식 파일이 없으면 명확한 오류 또는 기본 양식 fallback (work_order 지시).

---

## 8. 계정 작업 내역 시트 (선택)

- 정기점검 Export에 계정 작업 세부 내역이 필요하면 같은 workbook에 시트 추가.
- 시트명 예: `계정작업내역`
- 컬럼: 날짜 / 시스템구분 / 작업유형 / 작업자 / 대상자 / 세부내용
- 해당 월(work_date 기준) AccountWork를 필터해서 채움.